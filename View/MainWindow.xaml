<Window x:Class="ToastFish.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToastFish"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource Locator},Path=Main}"
        Title="MainWindow" Height="754.5" Width="409.5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="634.5"></RowDefinition>
            <RowDefinition Height="90"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Image x:Name="___ToastFish_component_IMG_3729_20210526_194228__JPG" Margin="0" Source="Pack://Application:,,,/Resources/chikaGroup.JPG" Stretch="Fill" Grid.ColumnSpan="2"/>
        </Grid>
        <Grid Grid.Row="1">
            <TextBlock Text="可以加群反馈BUG，提出意见建议。" Height="30" FontSize="22" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,0,0,0" />
            <TextBlock Text="（还可以一起摸鱼背单词）" Height="30" FontSize="20" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,30,0,0" />
        </Grid>
        <!--<Button Command="{Binding Push}" Content="Push"/>-->
    </Grid>
</Window>
